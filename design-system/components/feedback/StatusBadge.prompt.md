Domain-specific status pill tracking where a consigned item is in its lifecycle. Shown on inventory rows and consignor item lists.

```jsx
<StatusBadge status="sold" />
```

Stages: `received`, `priced`, `floor`, `sold`, `paid`, `returned`.
